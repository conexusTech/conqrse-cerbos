apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cerbos
  namespace: cerbos-base  # Will be replaced by overlays
  labels:
    app: cerbos
spec:
  ingressClassName: nginx
  rules:
  - host: cerbos.example.com  # Will be replaced by overlays
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: cerbos
            port:
              number: 3592
  # TLS configuration (uncomment and configure with certificate when ready)
  # tls:
  # - hosts:
  #   - cerbos.example.com
  #   secretName: cerbos-tls
