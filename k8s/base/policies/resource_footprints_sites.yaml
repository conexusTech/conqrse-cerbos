---
apiVersion: "api.cerbos.dev/v1"
resourcePolicy:
  version: "default"
  resource: "footprints:sites"
  importDerivedRoles:
    - conqrse_roles
  rules:
    # Retailer access with product subscription check
    - actions: ["resource:list", "resource:view", "resource:create", "resource:update", "resource:delete", "resource:export", "resource:import"]
      effect: EFFECT_ALLOW
      condition:
        match:
          expr: '"footprints" in P.attr.products && P.attr.retailerId == R.attr.retailerId'
      derivedRoles:
        - retailer_owner
        - retailer_manager
        - team_lead
        - staff_operator    # Agency member access to child retailer resources (check against agencyId)
    - actions: ["resource:list", "resource:view", "resource:export"]
      effect: EFFECT_ALLOW
      condition:
        match:
          expr: '"footprints" in P.attr.products && P.attr.agencyId == R.attr.agencyId'
      derivedRoles:
        - agency_member

    # Collaborators with product subscription check
    - actions: ["resource:list", "resource:view", "resource:export"]
      effect: EFFECT_ALLOW
      condition:
        match:
          expr: '"footprints" in P.attr.products && P.attr.agencyId == R.attr.agencyId'
      derivedRoles:
        - guest_collaborator