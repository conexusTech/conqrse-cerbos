---
apiVersion: "api.cerbos.dev/v1"
derivedRoles:
  name: conqrse_roles
  definitions:
    # Super User Level Roles (5)
    - name: root_user
      parentRoles: ["*"]
      condition:
        match:
          all:
            of:
              - expr: (P.attr.userLevel == "su")
              - expr: (P.attr.userType == "owner")

    - name: platform_administrator
      parentRoles: ["*"]
      condition:
        match:
          all:
            of:
              - expr: (P.attr.userLevel == "su")
              - expr: (P.attr.userType == "admin")

    - name: platform_lead
      parentRoles: ["*"]
      condition:
        match:
          all:
            of:
              - expr: (P.attr.userLevel == "su")
              - expr: (P.attr.userType == "lead")

    - name: platform_member
      parentRoles: ["*"]
      condition:
        match:
          all:
            of:
              - expr: (P.attr.userLevel == "su")
              - expr: (P.attr.userType == "member")

    - name: platform_collaborator
      parentRoles: ["*"]
      condition:
        match:
          all:
            of:
              - expr: (P.attr.userLevel == "su")
              - expr: (P.attr.userType == "collaborator")

    # Agency Level Roles (5)
    - name: agency_owner
      parentRoles: ["*"]
      condition:
        match:
          all:
            of:
              - expr: (P.attr.userLevel == "agency")
              - expr: (P.attr.userType == "owner")

    - name: agency_manager
      parentRoles: ["*"]
      condition:
        match:
          all:
            of:
              - expr: (P.attr.userLevel == "agency")
              - expr: (P.attr.userType == "admin")

    - name: agency_lead
      parentRoles: ["*"]
      condition:
        match:
          all:
            of:
              - expr: (P.attr.userLevel == "agency")
              - expr: (P.attr.userType == "lead")

    - name: agency_member
      parentRoles: ["*"]
      condition:
        match:
          all:
            of:
              - expr: (P.attr.userLevel == "agency")
              - expr: (P.attr.userType == "member")

    - name: agency_collaborator
      parentRoles: ["*"]
      condition:
        match:
          all:
            of:
              - expr: (P.attr.userLevel == "agency")
              - expr: (P.attr.userType == "collaborator")

    # Retailer Level Roles (5)
    - name: retailer_owner
      parentRoles: ["*"]
      condition:
        match:
          all:
            of:
              - expr: (P.attr.userLevel == "retailer")
              - expr: (P.attr.userType == "owner")

    - name: retailer_manager
      parentRoles: ["*"]
      condition:
        match:
          all:
            of:
              - expr: (P.attr.userLevel == "retailer")
              - expr: (P.attr.userType == "admin")

    - name: team_lead
      parentRoles: ["*"]
      condition:
        match:
          all:
            of:
              - expr: (P.attr.userLevel == "retailer")
              - expr: (P.attr.userType == "lead")

    - name: staff_operator
      parentRoles: ["*"]
      condition:
        match:
          all:
            of:
              - expr: (P.attr.userLevel == "retailer")
              - expr: (P.attr.userType == "member")

    - name: guest_collaborator
      parentRoles: ["*"]
      condition:
        match:
          all:
            of:
              - expr: (P.attr.userLevel == "retailer")
              - expr: (P.attr.userType == "collaborator")
